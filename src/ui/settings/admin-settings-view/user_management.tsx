import { Card, CardTitle } from "@/components/ui/card";
import { Separator } from "@/components/ui/separator";

export const UserManagementSettings = () => {
  return (
    <Card className="bg-background">
      <Separator />
      <CardTitle className="">Admin Management</CardTitle>
      <Separator />
    </Card>
  );
};
